# The Interest Cat

A tiny curiosity machine for GitHub Pages. Enter a topic or let the cat choose a fascinating corner of the web.

## Deploy

Push to `main` and the included GitHub Actions workflow publishes the site automatically. In the repository settings, set **Pages** to use **GitHub Actions** as the source.